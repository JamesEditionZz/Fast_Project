version: "3.9"
services:
  app:
    container_name: XS_Project
    restart: always
    build:
      context: ./
      dockerfile: Dockerfile
    volumes:
        - .:/app
        - /app/node_modules
        - /app/.next
    ports:
      - "4002:4002"